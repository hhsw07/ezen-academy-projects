1. 클래스 리스트
select * from ?(클래스+클래스종류);

	-코스 테이블에서
	클래스번호, 호스트, 클래스등록일?, 클래스명, 
	가격, 클래스이미지, 수업분류

1-2. 클래스 리스트 카테고리
select * from p04_course
where = 수업분류;


2. 클래스 상세
select 필요컬럼.. from p04_course;

	- 클래스 리스트에 들어갔던 부분 호출?+마일리지(마일리지테이블)


3. 클래스 소개
select 클래스번호, 상세설명 from p04_course
where = 클래스번호;

	- 코스 테이블에서
	상세설명 컬럼(img로 추가)

4. 클래스 후기(사원 정보 등록 처리 부분 확인)
입력 (아이디, 신청번호, 등록일이 insert에 넣어야하는지?)
insert into p04_cReview values(아이디(회원테이블),신청번호, 후기내용, 등록일);
결과
select 아이디(회원테이블),등록일,후기내용 !!!!여기서 조인이 필요한거 같은데?
 
	- 클래스리뷰 테이블에서
	신청번호, 후기내용, 평점, 등록일 + 아이디(회원테이블)

5. 클래스 교환/환불/배송 정책
	(모든 페이지에 동일하기때문에 따로 데이터로 값을 넣을 필요있는지?)
	- 정책부분은 필요없는지?

6. 장바구니
!!!!장바구니는 상세페이지에 있는 데이터를 장바구니로 이동시키는데 ...........?

	- 클래스 리스트에 들어갔던 부분중에(클래스상세와 같이 호출?)
	클래스이미지, 호스트, 클래스명, 가격
	장바구니 버튼 클릭시 클래스정보가 장바구니로 넘어가야 한다(어떻게?)

7. 구매하기
!!!!장바구니와 동일

	- 코스+회원+배송지 테이블정보
	코스 : 클래스이미지, 호스트, 클래스명, 가격
	회원 : 아이디 이름 이메일 휴대폰번호
	배송지 : 배송지번호, 배송지명, 수령인, 연락처, 우편번호, 주소, 상세주소
	구매하기 버튼 클릭시 클래스정보가 구매페이지로 넘어가야한다(어떻게?)

일단 erd기준으로 만든 sql테이블에 컬럼값을 전부 insert하고 필요한것만 
셀렉으로 불러온 후 vo객체를 만드는지?

+ 클래스 분류와 같이 하나에 컬럼에만 여러개의 값을 넣어야 할 때, 
 단일 행으로 추가해야하는지
