1. url의 구성요소를
http://192.168.0.23:7080/jspexp/a01_start.jsp?id=himan%pass=777
을 기준으로 구분하여 기술하세요.
	jspexp/a01_start.jsp : 대상 자원의 포드와 파일명
	?id=himan%pass=777 : 페이지에 전달하려는 요청값 key=value
2. jsp화면을 크게 구분하여, 상단/body에 들어갈 요소를 기술하세요.
	directive라고 지시자와 스크립트라고 실제 jsp코드를 지칭
3. 스크립트 요소 3가지를 구구단 프로그램을 기준으로 예를들어 설명하세요.
	-> 
	<form>
		첫번째 숫자 : <input type="text" name="num01"><br>
		두번째 숫자 : <input type="text" name="num02"><br>
		<input type="submit" value="곱하기">
	</form>
	
	<%
		String strNum01 = request.getParameter("num01");	-- 스클립트릿
		String strNum02 = request.getParameter("num02");
		int num01=0;
		if(strNum01!=null) num01=Integer.parseInt(strNum01);
		int num02=0;
		if(strNum02!=null) num02=Integer.parseInt(strNum02);
		if(strNum01!=null&&strNum02!=null){
						
	%>
		<h3>결과값 :<%=num01*num02 %></h3>	-- 표현식
	<%}%>
4. page 디렉티브 중에 ontentType은 어떤 역할을 하는 속성인지를 기술하세요.
	- > jsp가 생성할 문서의 타입을 지정함.
	
5. java로 클래스로 물건명, 가격, 갯수를 정의하여, jsp에서 활용하고자 한다.
	-> 해당 코드를 처리하는 순서와 코드를 기술하세요.
	 속성을 가진 java클래스를 src폴더에 패키지 지정, 선언
	 해당 패키지명과 클래스를 jsp코드에서 import로 페이지 디렉티브에서 선언
	 객체 생성과 활용
6. &name=홍길동&age=25&loc=강남 이라는 url매개변수 값을 jsp에서 처리하고자 한다.
	jsp 기본객체를 통해서 위 요청값을 처리하되 null일때는 문자열을 ""으로
	숫자는 0으로 처리하게 하여 h1에서 이름:@@@ 사는 곳:@@@ 이 화면에 나오게 코드하세요.
	-> <form>
		이름 : <input type="text" name="userName"><br>
		나이 : <input type="text" name="userAge"><br>
		사는 곳 : <input type="text" name="userLoc"><br>
		<input type="submit" value="입력하기">
	</form>
	
		<%
			String name = request.getParameter("userName");
		if(name==null) name="";
			String age = request.getParameter("userAge");
			int age=0;
		if(age!=null) age = Integer.getInteger(age);
			String loc = request.getParameter("userLoc");
		if(loc==null) loc="";
		{
		%>
		<h2>이름 :<%=name %></h2>
		<h2>나이 :<%=age %></h2>
		<h2>사는곳 :<%=loc %></h2>
		<%} %>
7. jsp 코드를 이용하여 3x3 테이블에 임의의 무지개 색상을 각 cell의 배경 
	색상으로 나타나게 for문으로 이용하여 처리하세요.
	-> <%
		String colors[] = { "red", "orange", "yellow", "green",
						"blue", "navy", "purple" };
	%>
	<table width="300px" height="300px" border>
		<%
		for(int idx=0; idx<9; idx++){
			int rIdx = (int)(Math.random()*colors.length);
			
			if(idx%3==0){
				
		%>
			<tr>
		<%} %>
			<td style="background-color:<%=colors[rIdx]%>;"><%=idx+1 %></td>
		<%
		if(idx%3==2){
			
		
			
		%>
			</tr>
		<%} %>
		<%} %>
	</table>
	
8. jsp의 내장 객체가 무엇인지 기술하세요.
	- jsp페이지 내부에서 제공하는 특수한 레퍼런스 타입의 변수
		request
		response
		out
		session
		application 
		page
		pageContext
		config
		exception
		