1. url의 구성요소를 
http://192.168.0.23:7080/jspexp/a01_start.jsp?id=himan&pass=777
를 기준으로 구분하여 기술하세요.
[프로토콜]://[호스트][:포트]/[경로][파일명][.확장자명][query string]

프로토콜 : http
호스트 : 192.169.0.23
:포트 : 7080
[경로][파일명][확장자명] /jspexp/a01_start.jsp
쿼리 스트링 : id=himan&pass=777
2. jsp화면을 크게 구분하여, 상단/body에 들어갈 요소를 기술하세요.
상단 : 디렉티브 지시자와 스크립트릿이라고 실제 jsp을 지칭한다.
3. 스크립트 요소 3가지를 구구단을 프로그램을 기준으로 예를들어 설명하세요.
<body>
	<h3>구구단~</h3>
	<%! int gugudan(int num1, int num2){
		return num1*num2;} %>
	
	<%
		for(int i = 2; i <= 9; i++){
			for(int j = 1; j<=9; j++){
				%>
			<h5><%= i %> * <%=j %> = <%=gugudan(i, j) %></h5> <% 
			}
		}
	%>

</body>

<%! %> = 함수 선언부
<% %> = 스크립트릿
<%= %> = 표현식

4. page 디렉티브 중에 ContentType은 어떤 역할을 하는 속성인지를 기술하세요.
contentType : jsp가 생성할 문서의 타입을 지정한다.
5. java로 클래스로 물건명, 가격, 갯수를 정의하여, jsp에서 활용하고자 한다.
	해당 코드를 처리하는 순서와 코드를 기술하세요.
	
	클래스 정의 -> jsp파일에서 import -> jsp에서 활용
	
	class Product{
		private String name;
		private int price;
		private int cnt;
		
		public Product(String name, int price, int cnt){
			this.name = name;
			this.price = price;
			this.cnt = cnt;
		}
		
				public String getName() {
				return name;
			}
		
			public void setName(String name) {
				this.name = name;
			}
		
			public int getPrice() {
				return Price;
			}
		
			public void setPrice(int price) {
				Price = price;
			}
		
			public int getCnt() {
				return Cnt;
			}
		
			public void setCnt(int cnt) {
				Cnt = cnt;
			}
		}
		
		jsp 파일 : 
		<%@ page import="jspexp.a01.Product"%>
		<% Product a01 = new Product("바나나", 1500, 1);%>
		<h1> 이름 : <%=a01.getName()%> 가격 : <%=a01.getPrice() %> 갯수 : <%=a01.getCnt()%>
6. &name=홍길동&age=25&loc=강남  이라는 url매개변수 값을 jsp에서 처리하고자 한다.
   jsp 기본객체를 통해서 위 요청값을 처리하되 null일때는 문자열을 ""으로,
   숫자는 0으로 처리되게 하여 h1에서 이름:@@@ 나이:@@ 사는 곳:@@이 화면에 
   나오게 코드하세요.
   <%
		String name = request.getParameter("name");
		int age;
		String ages = request.getParameter("age");
		String loc = request.getParameter("loc");
		
		if(name == null) name = "";
		if(ages == null){
			age = 0;
		} else {
			age = Integer.parseInt(ages);
		}
		
		if(loc == null) loc = "";
		
	
	%>
	<h1>이름 : <%= name %> 나이 = <%=age %> 주소 : <%=loc %></h1>
7. jsp 코드를 이용하여 3X3테이블에 임의의 무기개 색상을 각 cell의 배경
	색상으로 나타나게 for문으로 이용하여 처리하세요.
	<%String[] color = {"red","orange","yellow", "green", "blue","navy","purple"}; %>
	<table border>
		<%
			for(int cnt = 1; cnt <=9; cnt++){
				int index = (int)(Math.random()*7);
				if(cnt % 3 == 0){
					%>
					<td style="background-color: <%=color[index] %>"><%=cnt %>
					</td></tr><%
				} else if(cnt % 3 == 1){
					%><tr>
						<td style="background-color:<%=color[index] %>"><%=cnt %></td>
					<% 
				} else {
					%>
					<td style="background-color:<%=color[index] %>"><%=cnt %></td>
					<% 
				}
			}
		%>
	</table>
8. jsp의 내장 객체가 무엇인지 기술하세요.
내장 객체는 JSP 페이지 내에서 제공하는 특수한 레퍼런스 타입의 변수이다.

jsp 내장 객체의 종류 
response, request, session, out 등등
